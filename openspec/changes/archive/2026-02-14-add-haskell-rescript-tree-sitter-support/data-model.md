No new domain types — skipping data-model.

This change extends the existing `Lang` enum with two new variants (`Haskell`, `ReScript`) and adds match arms to existing functions. No new domain concepts, validated types, or state machines are introduced.
